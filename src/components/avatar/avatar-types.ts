export type AvatarProps = {
  // events?: Record<string, any>;
  img: string;
  name: string;
  inProfile: boolean;
};

export type AvatarAttribute = {
  class: string[];
  src: string;
  alt: string;
};
